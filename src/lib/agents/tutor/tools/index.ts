import { getAssessmentProgress } from "./get-assessment-progress";
import { getCourseStudyPack } from "./get-course-study-pack";
import { getQuizPerformance } from "./get-quiz-performance";
import { getUpcomingDeadlines } from "./get-upcoming-deadlines";
import { listEnrolledCourses } from "./list-enrolled-courses";

export const tutorTools = {
  listEnrolledCourses,
  getCourseStudyPack,
  getUpcomingDeadlines,
  getQuizPerformance,
  getAssessmentProgress,
};

export {
  listEnrolledCourses,
  getCourseStudyPack,
  getUpcomingDeadlines,
  getQuizPerformance,
  getAssessmentProgress,
};

